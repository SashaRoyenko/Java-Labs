input{
	file {
		path => "D:/KPI/JavaLabs/lab9/logs/*.log"
		start_position => "beginning"
	}
}
filter{
	grok {
		match => { "message" => "%{LOGLEVEL:LOGLEVEL} %{WORD:MARKER} %{TIMESTAMP_ISO8601:TIMESTAMP}" }
	}
}
output {
	elasticsearch {
		hosts => "localhost:9200"
		index => "sasharoy"
	}
}